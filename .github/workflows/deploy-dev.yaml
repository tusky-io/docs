name: Deploy to Dev

on:
  push:
    branches: [dev]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: tusky-io/docs

jobs:
  validate:
    name: Validate docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install -g mintlify@latest

      - run: mintlify broken-links

  build-and-push:
    name: Build & push image
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:dev-latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:dev-${{ github.sha }}

  deploy:
    name: Deploy to dev cluster
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > $HOME/.kube/config
          kubectl config use-context dev

      - name: Set image tag
        run: |
          cd k8s/overlays/dev
          kustomize edit set image ghcr.io/tusky-io/docs:dev-${{ github.sha }}

      - name: Deploy
        run: kubectl apply -k k8s/overlays/dev

      - name: Wait for rollout
        run: kubectl rollout status deployment/tusky-docs -n tusky-dev --timeout=300s

      - name: Get LoadBalancer URL
        run: |
          echo "Waiting for LoadBalancer external IP..."
          for i in $(seq 1 30); do
            IP=$(kubectl get svc tusky-docs -n tusky-dev -o jsonpath='{.status.loadBalancer.ingress[0].ip}{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null)
            if [ -n "$IP" ]; then
              echo "Docs available at: http://$IP"
              break
            fi
            sleep 10
          done
