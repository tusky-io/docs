---
title: Update Member
api: 'PUT https://api.tusky.io/v2/vaults/{vaultId}/members/{memberId}'
---

Update the role of an existing vault member.

## Path Parameters

<ParamField path="vaultId" type="string" required>
  The unique identifier of the vault.
</ParamField>

<ParamField path="memberId" type="string" required>
  The unique identifier of the member to update.
</ParamField>

## Request Body

<ParamField body="role" type="string" required>
  The new role to assign. One of:
  - `viewer` — read-only access to vault files
  - `contributor` — can upload, download, and delete files
  - `admin` — full access including member management
</ParamField>

## Response

<ResponseField name="id" type="string">
  Unique identifier for the membership.
</ResponseField>

<ResponseField name="address" type="string">
  The Sui wallet address of the member.
</ResponseField>

<ResponseField name="role" type="string">
  The updated role.
</ResponseField>

<ResponseField name="vaultId" type="string">
  The vault this membership belongs to.
</ResponseField>

<ResponseField name="createdAt" type="string">
  ISO 8601 timestamp of when the member was added.
</ResponseField>

## Example

<CodeGroup>

```bash cURL
curl -X PUT https://api.tusky.io/v2/vaults/vault_abc123def456/members/member_stu901vwx234 \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "role": "admin"
  }'
```

</CodeGroup>

### Response

```json
{
  "id": "member_stu901vwx234",
  "address": "0x1a2b3c4d5e6f7890abcdef1234567890abcdef12",
  "role": "admin",
  "vaultId": "vault_abc123def456",
  "createdAt": "2025-07-02T10:00:00.000Z"
}
```

<Note>
  Only vault owners and admins can update member roles. You cannot change the role of the vault owner.
</Note>
